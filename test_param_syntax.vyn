// Test both parameter syntax forms
fn<Int> add_colon(a: Int, b: Int) -> {
    return a + b;
}

fn<Int> add_angle(a<Int>, b<Int>) -> {
    return a + b;
}

fn<Int> main() -> {
    var<Int> result1 = add_colon(10, 20);
    var<Int> result2 = add_angle(30, 40);
    return result1 + result2;
}