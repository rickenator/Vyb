<!-- generated by tools/refman.vyb; do not edit -->
<!-- source: stdlib/ ; runtime: runtime/vyb_runtime.c ; commit: e83270fa71364b49f1af0c9b95369d58733358ab -->

# Shared types & consumers

Cross-module structs and enums — types defined in one module but imported, built, or handled by another. For each shared type: who imports/re-exports it and which functions use it in a signature.

| Type | Module | Consumers | Consumer modules |
|---|---|---|---|
| [`HttpResponse`](http.md#sym-httpresponse) | [`http`](http.md) | 1 | [`https`](https.md) |
| [`HttpTarget`](http.md#sym-httptarget) | [`http`](http.md) | 1 | [`https`](https.md) |
| [`File`](io.md#sym-file) | [`io`](io.md) | 1 | [`png`](png.md) |
| [`Socket`](network.md#sym-socket) | [`network`](network.md) | 2 | [`http`](http.md), [`websocket`](websocket.md) |
| [`TcpListener`](network.md#sym-tcplistener) | [`network`](network.md) | 1 | [`http`](http.md) |
| [`TcpStream`](network.md#sym-tcpstream) | [`network`](network.md) | 1 | [`http`](http.md) |
| [`TlsContext`](tls.md#sym-tlscontext) | [`tls`](tls.md) | 1 | [`https`](https.md) |
| [`TlsStream`](tls.md#sym-tlsstream) | [`tls`](tls.md) | 1 | [`https`](https.md) |

<a id="sym-http-httpresponse"></a>
## `HttpResponse` · struct

A completed HTTP round-trip. On success `status` is the parsed code and

- **Defined in** [`http`](http.md)

**Consumed by 1 module(s)**

- [`https`](https.md) — `https_get_full` uses it, `https_get_full_verified` uses it, `https_get_hops` uses it, `module` imports it (re-export)
<a id="sym-http-httptarget"></a>
## `HttpTarget` · struct

Resolved redirect target: scheme (http/https), host, port, and path. Port

- **Defined in** [`http`](http.md)

**Consumed by 1 module(s)**

- [`https`](https.md) — `module` imports it
<a id="sym-io-file"></a>
## `File` · struct

An open file. Only ever constructed valid; `path` records the target. The open

- **Defined in** [`io`](io.md)

**Consumed by 1 module(s)**

- [`png`](png.md) — `module` imports it
<a id="sym-network-socket"></a>
## `Socket` · enum

Domain / socket-type constants. A constant enum: each member is a compile-time

- **Defined in** [`network`](network.md)

**Consumed by 2 module(s)**

- [`http`](http.md) — `module` imports it
- [`websocket`](websocket.md) — `module` imports it
<a id="sym-network-tcplistener"></a>
## `TcpListener` · struct

- **Defined in** [`network`](network.md)

**Consumed by 1 module(s)**

- [`http`](http.md) — `http_accept` uses it, `http_listen` uses it, `http_local_port` uses it, `http_serve` uses it, `module` imports it
<a id="sym-network-tcpstream"></a>
## `TcpStream` · struct

- **Defined in** [`network`](network.md)

**Consumed by 1 module(s)**

- [`http`](http.md) — `http_accept` uses it, `http_close_stream` uses it, `http_read_head_stream` uses it, `http_respond_and_close` uses it, `http_send_all_stream` uses it, `http_serve_conn` uses it, `module` imports it
<a id="sym-tls-tlscontext"></a>
## `TlsContext` · struct

A holding SSL_CTX (client or server). Handles are opaque Ints.

- **Defined in** [`tls`](tls.md)

**Consumed by 1 module(s)**

- [`https`](https.md) — `module` imports it
<a id="sym-tls-tlsstream"></a>
## `TlsStream` · struct

A TLS session bound to an already-connected fd. `ssl` is the SSL* handle;

- **Defined in** [`tls`](tls.md)

**Consumed by 1 module(s)**

- [`https`](https.md) — `module` imports it, `tls_read_all` uses it, `tls_read_chunked` uses it, `tls_read_exact` uses it, `tls_read_line` uses it
