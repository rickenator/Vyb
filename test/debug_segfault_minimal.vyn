// Test to debug segfault - minimal Vec with immediate cleanup
main()<Int> -> {
    // Create a Vec that will trigger cleanup
    {
        v<Vec<Int32>> = Vec.new(1);
        println("Vec created");
    } // v goes out of scope here and should trigger cleanup
    
    println("After scope exit");
    return 0;
}