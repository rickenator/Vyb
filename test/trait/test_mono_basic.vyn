# Working test for monomorphization with explicit object literal types

struct Box<T> {
    value<T>
}

# Test that we can use Box<Int> successfully
main()<Int> -> {
    # This should work since we're not using generic impl yet
    # Just testing basic monomorphization
    return 42
}
