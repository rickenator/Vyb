// @test: Direct recursive struct rejected
// @description: Struct value fields cannot directly contain the same struct type
// @category: basic, semantic, struct
// @expect: fail
// @semantic-only: true
// @expect-error: cannot contain direct value field

struct Node {
    value<Int>,
    left<Node>,
    right<Node>
}

main() -> {
    return 0
}
