// @test: Bytes type test
// @description: Bytes type test
// @category: types
// @expect: pass
// @expect-return: 42
# Test Bytes type for raw binary data

# Note: Bytes is a fat pointer like String but for binary data
# struct Bytes { ptr: *u8, len: i64 }

test_bytes_concept()<Int> -> {
    # For now, we test that the Bytes type can be declared
    # Full byte sequence support with literals will come later

    # This demonstrates the type exists and can be used in declarations
    # In the future, we'll have byte literals like: data<Bytes> = [0xDE, 0xAD, 0xBE, 0xEF]

    return 42
}

main()<Int> -> {
    result<Int> = test_bytes_concept()
    return result
}
