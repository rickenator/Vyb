// Test comprehensive struct operations

struct Point {
    x<Int>,
    y<Int>
}

fn<Int> main() -> {
    var<Point> p1 = Point { x = 10, y = 20 };
    var<Point> p2 = Point { x = 5, y = 15 };
    
    var<Int> sum = p1.x + p1.y + p2.x + p2.y;
    return sum;
}