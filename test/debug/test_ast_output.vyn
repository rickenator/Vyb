// @expect: fail
// Simple test to show AST output format
struct Point {
    x: Int,
    y: Int
}

// This function doesn't need to compile, just parse to AST
test()<Point> -> {
    Point { x=10, y=20 }
}
