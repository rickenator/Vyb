// Test template constraint validation with trait bounds
template Container<T: Comparable> {
    struct Data {
        value<T>
    }
}

template Pair<T: Equatable, U: Hashable> {
    struct Data {
        first<T>,
        second<U>
    }
}

fn<Int> main() -> {
    // These instantiations should be valid based on built-in trait implementations
    
    // Int implements Comparable, so this should work
    // var<Container<Int>> int_container;
    
    // String implements Comparable, so this should work  
    // var<Container<String>> string_container;
    
    // Int implements Equatable, String implements Hashable
    // var<Pair<Int, String>> valid_pair;
    
    // For now, just return a simple value
    var<Int> result = 42;
    return result;
}