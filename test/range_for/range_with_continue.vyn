// Test: Range-based for loop with continue
main()<Int> -> {
    var<Int> sum = 0;
    for (i in 0..9) {
        if (i == 5) {
            continue;
        }
        sum = sum + i;
    }
    return sum;  // Should return 0+1+2+3+4+6+7+8+9 = 40
}
