// Test struct field access without returning the whole struct
// @expect-return: 10

struct Point {
    x<Int>,
    y<Int>
}

main()<Int> -> {
    p<Point> = Point { x = 10, y = 20 };
    return p.x;
}