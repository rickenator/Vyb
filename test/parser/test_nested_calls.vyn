// @expect-return: 20
multiply(i32 a, i32 b)<i32> -> {
    return a * b;
}

calculate(i32 x, i32 y)<i32> -> {
    sum<i32> = x + y;
    product<i32> = multiply(sum, 2);
    return product;
}

main()<i32> -> {
    result<i32> = calculate(3, 7);
    return result;
}