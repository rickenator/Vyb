// Test with variable assignment using angle bracket syntax
// @expect-return: 10
struct TestPoint {
    x<Int>,
    y<Int>
}

main()<Int> -> {
    p<TestPoint> = TestPoint { x=10, y=20 };
    return p.x;
}
