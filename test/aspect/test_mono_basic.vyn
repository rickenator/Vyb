// @expect-return: 42
# Working test -> monomorphization with explicit object literal types

struct Box<T> {
    value<T>
}

# Test that we can use Box<Int> successfully
main()<Int> -> {
    # This should work since we're not using generic bind yet
    # Just testing basic monomorphization
    return 42
}
