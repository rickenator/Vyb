# Simplified test -> monomorphization
# Tests that Box<Int> gets properly monomorphized

struct Box<T> {
    value<T>
}

main()<Int> -> {
    # Create a Box<Int> variable with explicit type in literal
    b<Box<Int>> = Box<Int> { value = 42 }
    return 0
}
