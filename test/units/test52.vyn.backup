//# EXPECTED ERROR: view() can only be used inside an unsafe block

main()<Int> -> {
    var<my<Int>> x = 10;
    
    // Using view outside unsafe block should fail
    var<their<Int const>> v = view(x);
    
    return x;
}
