// Memory semantics example with freedom, loc/at, and borrow/view.

main()<Int> -> {
    x<Int> = 42

    freedom {
        p<loc<Int>> = loc(x)
        at(p) = 64
    }

    b<their<Int>> = borrow(x)
    v<their<Int>> = view(x)

    println("memory value = " + x.to_string())
    return 0
}
