// Freedom block pointer example.

main()<Int> -> {
    x<Int> = 42

    freedom {
        p<loc<Int>> = loc(x)
        at(p) = 64
    }

    println("memory value = " + x.to_string())
    return 0
}
