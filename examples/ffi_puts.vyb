// FFI example: declare C functions at module scope, call them inside freedom.

extern "C" {
    puts(message<loc<Int8>>)<Int>
}

main()<Int> -> {
    freedom {
        puts("Hello from Vyb FFI")
    }
    return 0
}
